import model.Student;
import model.StudentDB;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
/*
public class TestStudentDB {
    @Test
    void testIfStudentIsAddedToArray() {

        //given

        Student[] studentArray = new Student[3];
        StudentDB studentDB = new StudentDB(studentArray);

        //when
        studentDB.addStudent(new Student("Theo", "123"));
        studentDB.addStudent(new Student("Lars", "1657"));
        studentDB.addStudent(new Student("Sara", "1345"));

        //then
        Assertions.assertNotNull(studentDB.getAllStudents());
        System.out.println(studentDB.toString());
    }
    }
*/